import 'package:flutter/material.dart';

class Bsection extends StatelessWidget {
  const Bsection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('B섹션'),
    );
  }
}
